File input sử dụng là các file: 
-> Sodiem_Tonghop_1xxx
-> kqht(x)
2 file phía trên phải được upload toàn bộ vào đúng vị trí trong thư mục input để phần mềm load


-> Nhap diem chi tiet mon XXXX -> Load một môn duy nhất của lớp ở dạng file này để lấy mã
-> so_diem_khoi_1x_mon_x_x -> Load toàn bộ dữ liệu file này để trả dữ liệu
2 loại file này sẽ phải được upload toàn bộ vào thư mục input để phần mềm lọc và xử lý
Trong 2 loại file này, ở file so_diem_khoi_1x_mon_x_x thì môn ngoại ngữ, tin học, công nghệ, thể dục, gdqp
không có đối với lớp tx. Tự tạo 2 sheet trắng này để chương trình không chạy lỗi.